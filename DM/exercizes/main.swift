func cleardomain(domain: inout [Int]) {
    for elements in 0 ..< domain.count {
        for checks in elements + 1 ..< domain.count {
            
          if domain[element] = domain] {
            
func isInjective(domain: [Int], range: [Int]) -> Bool {
    if domain.count <= range.count {
        return true
    }
}
      
func isBijective(range: [Int], domain: [Int]) -> Bool {
    if domain = range {
        return true
    }
}
func isSurjective(range: [Int], domain: [Int]) -> Bool {
    if range <= domain {
        return true
    }
}



